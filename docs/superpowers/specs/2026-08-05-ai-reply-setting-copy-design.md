# AI Reply Setting Copy Design

## Goal

Make the Agent Setup reply setting easier to understand.

## Approved UI

Keep the existing reply-mode selector and its behavior. Replace the section heading `Triggers` with `When should AI reply` and remove the separate `Reply mode` field label.

## Scope

Update the Agent Setup routing panel and its text regression. No routing behavior, permissions, loading states, or layout structure changes.
