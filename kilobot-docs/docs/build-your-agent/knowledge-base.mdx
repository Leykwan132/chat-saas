---
title: Knowledge Base
description: Add, maintain, and use the trusted information for a KiloBot agent.
slug: /build-your-agent/knowledge-base
---

import DocOutcomes from '@site/src/components/DocOutcomes';
import DocVerified from '@site/src/components/DocVerified';

# Knowledge Base

<DocVerified date="2026-07-28" />

Knowledge Base is the selected agent’s trusted source for business facts. Sources are agent-specific: confirm the correct agent before adding, replacing, or deleting information.

<DocOutcomes>

### By the end, you will

- Choose the right source type
- Add, replace, and remove trusted information
- Ground answers in approved facts

</DocOutcomes>

## Supported sources

| Source | Best for | Avoid when |
| --- | --- | --- |
| Web | Current public pages with related content | The site contains conflicting or private material |
| Files | Approved documents supported by the upload control | The document changes frequently |
| Text | A focused fact or policy that has no source page | The note would duplicate another source |
| Q&A | A direct question with a concise approved answer | Many questions need the same long document |

The upload control and **Storage limit** panel are authoritative for currently supported file formats, per-source limits, and plan allowance.

## Limits

Knowledge Base storage is measured per AI agent across its supported sources.

| Plan | Knowledge Base limit |
| --- | --- |
| Free | 400 KB per agent |
| Starter | 5 MB per agent |
| Growth | 20 MB per agent |
| Business | 40 MB per agent |
| Enterprise | Custom per agent |

## Best practice

### Convert PDFs to text

When a PDF contains important business facts, convert it to clean text first and add that text as a source. Text is easier to review and often gives the agent more reliable grounding than extracting directly from a PDF.

:::warning
Extracting information from an image or PDF is not 100% accurate. Scans, tables, columns, and formatting can be missed or misread, so review converted text and confirm important answers before relying on it.
:::

## Common problems

- **The model answer has not updated:** It normally takes a couple of minutes for the model to extract the data and update its knowledge, so allow time for processing to finish.
- **The answer is not fixed:** For a fixed response, use a [Workflow](/automate/send-messages-and-assets) because you control the exact message it sends.
- **The answer is summarized:** Anything added to the Knowledge Base is subject to summarization by the model. Use a Workflow when the wording, assets, or timing must stay exact.

## Next steps

- [Refine Agent Setup](/build-your-agent/agent-setup) with clear behavior and boundaries.
- [Send messages and assets](/automate/send-messages-and-assets) when a request needs an action.
- [Deploy to a channel](/channels/website-widget) to use the agent with real visitors.
