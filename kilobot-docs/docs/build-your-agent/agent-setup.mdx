---
title: Agent Setup
description: Define, test, and publish the behavior of your KiloBot agent.
slug: /build-your-agent/agent-setup
---

import DocCard from '@site/src/components/DocCard';
import DocExample from '@site/src/components/DocExample';
import DocMediaPlaceholder from '@site/src/components/DocMediaPlaceholder';
import DocSuccess from '@site/src/components/DocSuccess';
import DocOutcomes from '@site/src/components/DocOutcomes';
import DocVerified from '@site/src/components/DocVerified';

# Agent Setup

<DocVerified date="2026-07-28" />

Agent Setup defines who the agent is, how it communicates, and when it should involve a person. Keep business facts in [Knowledge Base](/build-your-agent/knowledge-base) and use [Workflows](/automate/workflow-overview) for requests that must perform an action.

<DocOutcomes>

### By the end, you will

- Decide where instructions, facts, and actions belong
- Write clear behavior, tone, boundaries, and escalation rules
- Test changes before publishing the agent

</DocOutcomes>

## Decide where information belongs

| Put it in | Use it for | Example |
| --- | --- | --- |
| Agent Setup | Role, goals, tone, boundaries, escalation | “Never diagnose a medical condition.” |
| Knowledge Base | Durable facts the agent may answer from | Opening hours, pricing, policies |
| Workflow | A defined condition that performs an action | Send a brochure or book an appointment |

## Write the system prompt

A useful prompt identifies the agent, audience, goals, conversation behavior, knowledge limits, and handoff boundary.

<DocExample title="Weak prompt">

`You are a helpful dental assistant. Answer questions and get bookings.`

This leaves the audience, knowledge boundary, escalation behavior, and acceptable booking behavior unclear.

</DocExample>

<DocExample title="Northstar Booking Assistant system prompt">

```text
You are Northstar Booking Assistant for Northstar Dental.

Help prospective and existing patients understand approved clinic information
and take the next appropriate step.

Use the Knowledge Base for business facts. If the answer is missing or
conflicting, say that you do not have confirmed information and offer a human
handoff. Never invent services, prices, availability, medical advice, diagnoses,
or treatment suitability.

Ask one clear question at a time. Keep answers concise, warm, and professional.
Use a Workflow when a matching request requires a brochure, booking, escalation,
or conversation closure.

Escalate urgent symptoms, complaints, payment disputes, privacy requests, and
requests for clinical judgment to a teammate.
```

</DocExample>

<ol className="steps">
  <li>Open <strong>Agent Setup</strong>.</li>
  <li>Apply a starter template or replace the prompt with an approved version.</li>
  <li>Keep facts out of the prompt when they belong in Knowledge Base.</li>
  <li>Review the escalation and safety boundaries.</li>
</ol>

## Choose model and style

Open **Model & Style**, choose a model available to the workspace plan, and set response length, emoji use, formality, and humor. Style settings shape normal replies; exact Workflow messages remain exact.

## Control AI replies

Open **Triggers** and choose the available **Reply mode**. Human escalation or a conversation-level pause can prevent automatic replies even when the agent is published. Missing controls can indicate a plan, permission, or routing restriction.

<DocMediaPlaceholder
  kind="image"
  title="Agent Setup controls"
  description="Show the prompt, Model & Style, Triggers, Test agent, and Publish controls in one readable composition."
  capture={['Prompt editor', 'Model & Style section', 'Triggers and Reply mode', 'Test agent and Publish actions']}
  callouts={['Prompt', 'Style', 'Reply mode', 'Test', 'Publish']}
  sensitive={['Private prompt rules', 'Unrelated agent names']}
  assetPath="/media/agent/agent-setup-controls.png"
/>

## Test the behavior

Test customer wording, abbreviations, and incomplete questions—not only phrases copied from your source material.

| Test | Northstar message | Expected behavior |
| --- | --- | --- |
| Known fact | “Are you open Saturday?” | Uses the approved hours |
| Paraphrase | “Can I come this weekend?” | Reaches the same grounded answer |
| Missing fact | “Do you offer emergency root canals?” | Does not invent a service |
| Escalation | “I want to dispute a payment.” | Offers or triggers human help |
| Workflow intent | “Send me your brochure.” | Matches the intended Workflow only |

<DocMediaPlaceholder
  kind="image"
  title="Test grounded and unknown questions"
  description="Show one supported answer and one safe response to missing knowledge."
  capture={['A grounded opening-hours answer', 'An unknown service question', 'The non-invented response']}
  callouts={['Grounded', 'Unknown', 'Safe boundary']}
  sensitive={['Other test conversations']}
  assetPath="/media/agent/test-agent.png"
/>

## Publish

**Publish** makes the current draft active. Saving or editing a field does not guarantee that customers receive the new configuration. If you navigate away with an unpublished draft, return and publish after testing.

<DocSuccess>

- Known and paraphrased questions use the same approved facts.
- Missing information is not invented.
- Escalation and Workflow tests activate only when intended.
- The latest tested draft has been published.

</DocSuccess>

## Common problems

- **The answer is wrong:** correct conflicting Knowledge Base sources before expanding the prompt.
- **An action does not run:** check the Workflow condition and near-miss tests.
- **AI does not reply:** check conversation AI state, Reply mode, credits, permissions, and channel connection.
- **A change is absent:** confirm the draft was published.

<DocCard to="/build-your-agent/knowledge-base" title="Knowledge Base" description="Add and maintain the trusted facts used in replies" />
