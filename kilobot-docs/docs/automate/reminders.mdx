---
title: Reminders
description: Schedule approved WhatsApp messages before booked appointments.
slug: /automate/reminders
---

import DocExample from '@site/src/components/DocExample';
import DocMediaPlaceholder from '@site/src/components/DocMediaPlaceholder';
import DocOutcomes from '@site/src/components/DocOutcomes';
import DocVerified from '@site/src/components/DocVerified';

# Reminders

<DocVerified date="2026-07-28" />

Reminders help you send appointment-time WhatsApp messages. Use them for appointment timing; use **Follow-ups** when the trigger is a customer who has not replied. Create and approve the template for closed-window sends under [Message templates](/engage/message-templates).

<DocOutcomes>

### By the end, you will

- Choose which appointments should receive reminders
- Configure approved messages and send timing
- Activate reminders for eligible appointments

</DocOutcomes>

:::important
Meta bills these message charges directly. Set up a valid payment method in your Meta Business account before activating Reminders. KiloBot is not where you add that card or top up Meta message credit.
:::

## Choose which appointments to include

| Scope | Includes | Best for |
| --- | --- | --- |
| **Future only** | Eligible appointments created after activation | A clean, low-risk launch |
| **Current & future** | Remaining eligible sends for current upcoming appointments and new appointments | Applying the automation to an already-reviewed calendar |

This choice controls which appointments enter scheduling. It does not modify the appointments.

<ol className="steps">
  <li>Open <strong>Workflows → Reminders</strong>.</li>
  <li>Choose which appointments to include.</li>
  <li>Choose how many reminders to send and when each should be sent.</li>
  <li>Select an approved WhatsApp template and choose <strong>Confirm</strong>.</li>
  <li>Review the Summary, then turn Reminders on.</li>
</ol>

<DocExample title="Northstar Consultation">

Send one reminder 24 hours before the appointment and a second reminder 2 hours before it. Start with **Future only** while verifying the workflow.

</DocExample>

<DocMediaPlaceholder
  kind="image"
  title="Completed Reminder configuration"
  description="Show a two-reminder Northstar setup immediately before activation."
  capture={[
    'Which appointments to include',
    'Both reminder timings',
    'Selected approved template',
    'Summary and Active switch',
  ]}
  callouts={[
    'Future only is the safest first activation',
    'Confirm the template selection',
    'Review the Summary before turning it on',
  ]}
  sensitive={['Customer information', 'Phone numbers', 'Private template variables']}
  assetPath="/img/guides/workflows/reminder-configuration.png"
/>

## Pricing

Meta bills WhatsApp message charges directly to your Meta Business account. Set up a payment method there before activating Reminders. KiloBot credits are separate, and KiloBot is not where you add a card or top up Meta message credit. See the [official WhatsApp pricing](https://whatsappbusiness.com/products/platform-pricing/) for current rates.

## Next steps

- [Create message templates](/engage/message-templates) for approved appointment reminders.
- [Set up Follow-ups](/automate/follow-ups) for no-reply customer follow-up.
- [Automate bookings](/automate/automate-bookings) so customers can choose an available slot.
